# Chandra OCR API Service

Локальный REST API сервис для распознавания документов на базе [Chandra OCR](https://github.com/datalab-to/chandra).

## 🌟 Особенности

- ✅ **Русский язык** - отличная поддержка русского текста + латиница в наименованиях
- 📄 **Множество форматов** - PDF, JPG, PNG, BMP, TIFF, WebP
- 🎯 **Высокая точность** - Chandra лидирует в бенчмарках (83.1% на olmOCR)
- 📊 **Сложные документы** - таблицы, формы, рукописный текст, математика
- 🔒 **Локальное выполнение** - данные не покидают ваш сервер
- 🚀 **Простой REST API** - интеграция с любым языком программирования
- 📝 **Подробное логирование** - отслеживание всех операций
- 🐳 **Без Docker** - работает напрямую в системе

## 📋 Требования

- **Python**: 3.10 или выше
- **ОС**: Linux (Ubuntu 20.04+ рекомендуется)
- **RAM**: минимум 8 ГБ (16 ГБ рекомендуется)
- **GPU** (опционально): NVIDIA с CUDA для ускорения

## 🚀 Быстрый старт

### 1. Автоматическая установка

```bash
cd /data/chandraocr
chmod +x install.sh
./install.sh
```

### 2. Активация виртуального окружения

```bash
source .venv/bin/activate
```

### 3. Запуск сервера

```bash
python main.py
```

Или через uvicorn:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 4. Проверка работы

```bash
# Проверка здоровья сервиса
curl http://localhost:8000/health

# Информация об API
curl http://localhost:8000/
```

## 📦 Ручная установка

Если автоматический скрипт не подходит:

```bash
# 1. Создание виртуального окружения
python3 -m venv .venv
source .venv/bin/activate

# 2. Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

# 3. Создание директорий
mkdir -p temp logs

# 4. Копирование конфигурации
cp .env.example .env

# 5. (Опционально) Для GPU установите flash-attention
pip install flash-attn
```

## 🔧 Конфигурация

Отредактируйте файл `.env`:

```bash
# Настройки сервера
HOST=0.0.0.0              # IP адрес (0.0.0.0 = все интерфейсы)
PORT=8000                 # Порт сервера
LOG_LEVEL=INFO            # Уровень логирования (DEBUG, INFO, WARNING, ERROR)

# Ограничения
MAX_FILE_SIZE=104857600   # Макс. размер файла в байтах (100 МБ)
OCR_TIMEOUT=600           # Таймаут обработки в секундах (10 минут)

# Метод OCR
DEFAULT_METHOD=hf         # hf (локальный) или vllm (через сервер)

# Настройки модели
MODEL_CHECKPOINT=datalab-to/chandra
MAX_OUTPUT_TOKENS=8192
```

## 📡 API Endpoints

### 1. POST /ocr - Распознавание документа

Основной endpoint для OCR. Возвращает распознанный текст в формате Markdown.

**Параметры:**
- `file` (обязательный) - файл документа
- `method` (опциональный) - метод обработки: `hf` (по умолчанию) или `vllm`
- `include_images` (опциональный) - извлекать изображения из документа (bool)
- `include_headers` (опциональный) - включать колонтитулы (bool)

**Пример:**

```bash
# Простой запрос
curl -X POST "http://localhost:8000/ocr" \
     -F "file=@document.pdf" \
     --output result.txt

# С параметрами
curl -X POST "http://localhost:8000/ocr" \
     -F "file=@scan.jpg" \
     -F "method=hf" \
     -F "include_images=true" \
     -F "include_headers=false" \
     --output result.txt

# Через Python
import requests

with open('document.pdf', 'rb') as f:
    response = requests.post(
        'http://localhost:8000/ocr',
        files={'file': f},
        data={'method': 'hf'}
    )
    text = response.text
    print(text)
```

### 2. POST /ocr/json - OCR с метаданными

Возвращает JSON с текстом и дополнительной информацией.

**Ответ:**
```json
{
  "success": true,
  "text": "Распознанный текст в Markdown...",
  "html": "HTML версия (если доступна)",
  "metadata": {
    "pages": 3,
    "tokens": 1523
  },
  "images_count": 5,
  "processing_time": 12.34,
  "file_size": 1048576,
  "filename": "document.pdf"
}
```

**Пример:**

```bash
curl -X POST "http://localhost:8000/ocr/json" \
     -F "file=@document.pdf" \
     | jq .
```

### 3. GET /health - Проверка здоровья

Проверяет работоспособность сервиса и доступность Chandra.

```bash
curl http://localhost:8000/health
```

**Ответ:**
```json
{
  "status": "healthy",
  "chandra_available": true,
  "version": "1.0.0",
  "temp_dir": "/data/chandraocr/temp",
  "temp_dir_exists": true
}
```

### 4. GET /docs - Swagger документация

Интерактивная документация API:
```
http://localhost:8000/docs
```

### 5. GET /redoc - ReDoc документация

Альтернативная документация:
```
http://localhost:8000/redoc
```

## 🎯 Примеры использования

### Bash/cURL

```bash
# Распознать PDF
curl -X POST http://localhost:8000/ocr \
     -F "file=@invoice.pdf" \
     --output invoice.txt

# С метаданными в JSON
curl -X POST http://localhost:8000/ocr/json \
     -F "file=@contract.pdf" \
     | jq '.text' > contract.txt
```

### Python

```python
import requests

# Простое распознавание
def ocr_document(file_path):
    with open(file_path, 'rb') as f:
        response = requests.post(
            'http://localhost:8000/ocr',
            files={'file': f}
        )
    return response.text

# С дополнительными параметрами
def ocr_with_options(file_path):
    with open(file_path, 'rb') as f:
        response = requests.post(
            'http://localhost:8000/ocr/json',
            files={'file': f},
            data={
                'method': 'hf',
                'include_images': True,
                'include_headers': False
            }
        )
    return response.json()

# Использование
text = ocr_document('scan.pdf')
print(text)

result = ocr_with_options('document.jpg')
print(f"Текст: {result['text'][:100]}...")
print(f"Время обработки: {result['processing_time']}s")
print(f"Найдено изображений: {result['images_count']}")
```

### JavaScript/Node.js

```javascript
const FormData = require('form-data');
const fs = require('fs');
const axios = require('axios');

async function ocrDocument(filePath) {
  const form = new FormData();
  form.append('file', fs.createReadStream(filePath));
  
  const response = await axios.post(
    'http://localhost:8000/ocr',
    form,
    { headers: form.getHeaders() }
  );
  
  return response.data;
}

// Использование
ocrDocument('document.pdf')
  .then(text => console.log(text))
  .catch(err => console.error(err));
```

### 1C:Enterprise (через HTTPСоединение)

```1c
Функция РаспознатьДокумент(ПутьКФайлу)
    Соединение = Новый HTTPСоединение("localhost", 8000);
    Запрос = Новый HTTPЗапрос("/ocr");
    
    ДанныеФайла = Новый ДвоичныеДанные(ПутьКФайлу);
    Файлы = Новый Соответствие;
    Файлы.Вставить("file", ДанныеФайла);
    
    Ответ = Соединение.ОтправитьДляОбработки(Запрос, Файлы);
    
    Если Ответ.КодСостояния = 200 Тогда
        Возврат Ответ.ПолучитьТелоКакСтроку();
    Иначе
        ВызватьИсключение "Ошибка OCR: " + Ответ.КодСостояния;
    КонецЕсли;
КонецФункции

Текст = РаспознатьДокумент("C:\Документы\счет.pdf");
Сообщить(Текст);
```

## 🔍 Поддерживаемые форматы

- **PDF** - многостраничные документы
- **JPG/JPEG** - фотографии и сканы
- **PNG** - изображения с прозрачностью
- **BMP** - растровые изображения
- **TIFF/TIF** - многослойные документы
- **WebP** - современный формат изображений

## 🌍 Поддержка языков

Chandra поддерживает 40+ языков, включая:
- ✅ Русский (отличная поддержка)
- ✅ Английский
- ✅ Немецкий, Французский, Испанский
- ✅ Китайский, Японский, Корейский
- ✅ Арабский, Иврит

**Язык определяется автоматически** - не нужно указывать явно.

## 📊 Производительность

### На CPU (без GPU):
- Простая страница A4: ~30-60 секунд
- Сложная таблица: ~60-120 секунд
- Многостраничный PDF (10 страниц): ~5-10 минут

### На GPU (NVIDIA):
- Простая страница A4: ~5-10 секунд
- Сложная таблица: ~10-20 секунд
- Многостраничный PDF (10 страниц): ~1-2 минуты

### Ускорение через vLLM:
Для продакшена рекомендуется использовать vLLM сервер:

```bash
# Запуск vLLM сервера
chandra_vllm

# В .env укажите
DEFAULT_METHOD=vllm
```

## 📝 Логирование

Все операции записываются в лог:

```bash
# Просмотр логов
tail -f logs/chandra_ocr.log

# Поиск ошибок
grep ERROR logs/chandra_ocr.log

# Статистика по запросам
grep "Новый запрос OCR" logs/chandra_ocr.log | wc -l
```

Формат лога:
```
2025-11-16 15:30:45 - __main__ - INFO - [20251116_153045_123456] Новый запрос OCR: document.pdf, method=hf
2025-11-16 15:30:47 - __main__ - INFO - [20251116_153045_123456] Файл сохранен: input.pdf, размер: 1048576 байт
2025-11-16 15:30:52 - __main__ - INFO - [20251116_153045_123456] OCR выполнен за 5.12s
2025-11-16 15:30:52 - __main__ - INFO - [20251116_153045_123456] OCR завершен: 1234 символов, 0 изображений, 5.12s
```

## 🛡️ Безопасность

### Рекомендации для production:

1. **Ограничение доступа**
   ```bash
   # В .env укажите
   HOST=127.0.0.1  # Только локальный доступ
   ```

2. **Настройка firewall**
   ```bash
   # Разрешить только с определенных IP
   sudo ufw allow from 192.168.1.0/24 to any port 8000
   ```

3. **Использование reverse proxy (nginx)**
   ```nginx
   location /ocr/ {
       proxy_pass http://127.0.0.1:8000/;
       proxy_set_header Host $host;
       client_max_body_size 100M;
   }
   ```

4. **HTTPS через nginx**
   ```nginx
   server {
       listen 443 ssl;
       ssl_certificate /path/to/cert.pem;
       ssl_certificate_key /path/to/key.pem;
       
       location / {
           proxy_pass http://127.0.0.1:8000;
       }
   }
   ```

5. **Rate limiting**
   - Используйте nginx для ограничения запросов
   - Или добавьте middleware в FastAPI

## 🔧 Troubleshooting

### Проблема: "Chandra недоступна"

```bash
# Проверьте установку
source .venv/bin/activate
chandra --help

# Переустановите если нужно
pip install --upgrade chandra-ocr
```

### Проблема: "Out of memory"

```bash
# Уменьшите MAX_OUTPUT_TOKENS в .env
MAX_OUTPUT_TOKENS=4096

# Или обрабатывайте меньшие файлы
MAX_FILE_SIZE=52428800  # 50 МБ
```

### Проблема: Медленная обработка

```bash
# 1. Установите flash-attention (для GPU)
pip install flash-attn

# 2. Используйте vLLM сервер
chandra_vllm

# 3. В .env укажите
DEFAULT_METHOD=vllm
```

### Проблема: Ошибка при обработке PDF

```bash
# Проверьте PDF
pdfinfo document.pdf

# Попробуйте конвертировать в изображения
pdftoppm document.pdf output -png

# Обработайте изображения вместо PDF
```

## 🚀 Запуск как системный сервис (systemd)

Создайте файл `/etc/systemd/system/chandra-ocr.service`:

```ini
[Unit]
Description=Chandra OCR API Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/data/chandraocr
Environment="PATH=/data/chandraocr/.venv/bin"
ExecStart=/data/chandraocr/.venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl daemon-reload
sudo systemctl enable chandra-ocr
sudo systemctl start chandra-ocr
sudo systemctl status chandra-ocr
```

## 📚 Дополнительные ресурсы

- [Chandra GitHub](https://github.com/datalab-to/chandra)
- [Chandra на HuggingFace](https://huggingface.co/datalab-to/chandra)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Datalab официальный сайт](https://www.datalab.to/)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `logs/chandra_ocr.log`
2. Проверьте здоровье сервиса: `curl http://localhost:8000/health`
3. Изучите примеры выше
4. Создайте issue в репозитории Chandra

## 📄 Лицензия

- **Код API**: Используйте свободно
- **Chandra код**: Apache 2.0
- **Chandra модель**: Modified OpenRAIL-M (бесплатно для стартапов <$2M, исследований, личного использования)

## ⚡ Быстрая справка

```bash
# Установка
./install.sh

# Запуск
source .venv/bin/activate
python main.py

# Проверка
curl http://localhost:8000/health

# Распознавание
curl -X POST http://localhost:8000/ocr \
     -F "file=@document.pdf" \
     --output result.txt

# Просмотр логов
tail -f logs/chandra_ocr.log
```

Готово! 🎉
